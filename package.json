{
  "name": "echolink",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ rabbit-do/echolink",
    "create-local": "graph create --node http://localhost:8020/ rabbit-do/echolink",
    "remove-local": "graph remove --node http://localhost:8020/ rabbit-do/echolink",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rabbit-do/echolink"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
